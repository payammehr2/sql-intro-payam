-- Get the first and last name of every player in the database.

SELECT first_name, last_name FROM players;
-- sqlite3 baseball.sqlite3
-- .schema
-- .read
-- .quit
-- .mode columns
-- .headers on

-- SELECT x, y, z FROM mmmmm 
-- WHERE wins > 100
-- AND park = "xx"
-- ORDER BY year DESC
-- LIMIT 3

-- SELECT product, COUNT(*)
-- FROM reviews
-- GROUP BY products;

-- Count(*) SUM(columns) MAX(columns) MIN(columns) SUM(columns)




